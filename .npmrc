# Vortyx — local pnpm config
# 1) Disable the minimum-release-age supply-chain policy for this project.
# 2) Skip the implicit lockfile check before `pnpm exec` / `pnpm run`.
minimum-release-age=0
verify-deps-before-run=false
